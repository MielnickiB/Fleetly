@page "/orders/{orderId:int}/protocol/{protocolType:string}"
@inherits PageBase<ProtocolResponseDto>
@inject IProtocolService ProtocolService

@code {
    protected override async Task LoadDataAsync()
    {
        
    }
}
